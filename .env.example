# BYOK is required by default so public users never consume the product owner's key.
EXPRESSION_REQUIRE_BYOK=true
# Public GitHub version defaults to BYOK. Host-funded beta is explicit and password-gated.
EXPRESSION_MODEL_MODE=byok
EXPRESSION_BETA_PASSWORD=
EXPRESSION_SPONSORED_DAILY_LIMIT=100
EXPRESSION_BYOK_TTL_SECONDS=28800

# Optional private fallback for controlled deployments only.
EXPRESSION_LLM_PROVIDER=openai
EXPRESSION_LLM_BASE_URL=https://api.openai.com/v1
EXPRESSION_LLM_API_KEY=
EXPRESSION_LLM_MODEL=
EXPRESSION_LLM_TIMEOUT_SECONDS=30
EXPRESSION_LLM_RETRIES=1

# Optional speech model overrides.
FUNASR_MODEL=paraformer-zh
FUNASR_VAD_MODEL=fsmn-vad
FUNASR_PUNC_MODEL=ct-punc
EXPRESSION_FFMPEG_PATH=

# Optional operator access, separate from every model key. Empty disables admin login.
# At least 32 random characters. This file is a template and is not loaded automatically.
EXPRESSION_ADMIN_TOKEN=
# Enable only behind an HTTPS deployment; localhost development uses false.
EXPRESSION_SECURE_COOKIE=false
